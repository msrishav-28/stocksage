# Base URL of the StockSage API.
# Leave blank for local development — the CRA dev server proxies /api and
# /health to http://127.0.0.1:8000 (see "proxy" in package.json).
# Set this for production builds, e.g. https://stocksage-api.example.com
REACT_APP_API_URL=
