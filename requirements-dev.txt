# Development & testing dependencies.
# Installs the full runtime set plus test/lint tooling.
#   pip install -r requirements-dev.txt
-r requirements.txt

pytest==8.3.2
pytest-asyncio==0.23.8
pytest-cov==5.0.0
ruff==0.5.7
